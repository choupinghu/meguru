# Neon Postgres connection string (pooled). Get it from the Neon dashboard
# (Connection Details -> pooled connection). Copy this file to .env.local
# and paste the real value there -- never commit .env.local.
DATABASE_URL=
